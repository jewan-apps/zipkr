package com.jewan.zipkr.ui.components

/**
 * 사용자가 방금 복사한 항목을 식별한다.
 * AddressResultCard·DetailHeader가 이 값으로 본문 텍스트 한 줄을 시각 highlight한다 — "어떤 내용이 클립보드에 들어갔는지" 즉시 인지.
 */
enum class CopyField {
    Zip,
    Road,
    Jibun,
    English,
}
