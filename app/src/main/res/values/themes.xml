<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Zipkr" parent="android:Theme.Material.Light.NoActionBar" />

    <!--
        Splash Screen API용 launch theme이다.
        - postSplashScreenTheme로 본 Theme.Zipkr을 지정해 splash 종료 후 컴포즈 화면이 본 테마로 그려진다.
        - animatedIcon은 adaptive icon foreground (drawable 리소스)를 그대로 재사용한다.
    -->
    <style name="Theme.Zipkr.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Zipkr</item>
    </style>
</resources>
